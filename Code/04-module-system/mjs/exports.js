export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

const theExport = "This is a great package";

export default theExport;

// module.exports = {
//   add: add,
//   subtract: subtract,
// };
